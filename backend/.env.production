# Production Environment Variables
# Copy these to your Railway dashboard

NODE_ENV=production
PORT=3001

# Database - Railway PostgreSQL
# Railway will automatically inject DATABASE_URL
# These are extracted from DATABASE_URL if needed
DB_HOST=${PGHOST}
DB_PORT=${PGPORT}
DB_NAME=${PGDATABASE}
DB_USER=${PGUSER}
DB_PASSWORD=${PGPASSWORD}

# JWT Configuration
# IMPORTANT: Generate a secure random string for production!
# Use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=PLEASE_CHANGE_THIS_TO_SECURE_RANDOM_STRING_IN_PRODUCTION
JWT_EXPIRES_IN=7d

# CORS Configuration
# Update this with your Vercel deployment URL
CORS_ORIGIN=https://your-app.vercel.app

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# File Upload
MAX_FILE_SIZE=10485760
UPLOAD_DIR=./uploads

# API Keys (if needed)
GEMINI_API_KEY=your_gemini_api_key_if_using_backend_ai
